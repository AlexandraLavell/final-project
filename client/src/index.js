import React from 'react';
import ReactDOM from 'react-dom';

// main App component
import App from './App';

// main render
ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
